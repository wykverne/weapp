<!--pages/index/index.wxml-->
<view class="container">
  <view class="userinfo">
    <block wx:if="{{canIUseOpenData}}">
      <view class="userinfo-avatar" bindtap="bindViewTap">
        <open-data type="userAvatarUrl"></open-data>
      </view>
      <open-data type="userNickName"></open-data>
    </block>
    <block wx:elif="{{!hasUserInfo}}">
      <button wx:if="{{canIUseGetUserProfile}}" bindtap="getUserProfile"> 获取头像昵称 </button>
      <button wx:elif="{{canIUse}}" open-type="getUserInfo" bindgetuserinfo="getUserInfo"> 获取头像昵称 </button>
      <view wx:else> 请使用1.4.4及以上版本基础库 </view>
    </block>
    <block wx:else>
      <image bindtap="bindViewTap" class="userinfo-avatar" src="{{userInfo.avatarUrl}}" mode="cover"></image>
      <text class="userinfo-nickname">{{userInfo.nickName}}</text>
    </block>
  </view>
  <view class="usermotto">
    <text class="user-motto">{{motto}}</text>
  </view>
  
  <view class="welcome-section">
    <view class="title">欢迎使用微信小程序</view>
    <view class="subtitle">这是一个基础的小程序模板</view>
    
    <view class="feature-list">
      <view class="feature-item">
        <text class="feature-icon">📱</text>
        <text class="feature-text">跨平台运行</text>
      </view>
      <view class="feature-item">
        <text class="feature-icon">⚡</text>
        <text class="feature-text">快速开发</text>
      </view>
      <view class="feature-item">
        <text class="feature-icon">🎨</text>
        <text class="feature-text">丰富组件</text>
      </view>
    </view>
  </view>

  <view class="welcome-section">
    <view class="title">无痕聊天</view>
    <view class="feature-list">
      <view class="feature-item">
        <button type="primary" bindtap="createRoom">建立房间</button>
      </view>
      <view class="feature-item">
        <input class="input" type="number" placeholder="房间号" bindinput="onJoinRoomIdInput" value="{{joinRoomId}}" />
      </view>
      <view class="feature-item">
        <input class="input" type="number" placeholder="数字密码" bindinput="onJoinRoomPasswordInput" value="{{joinRoomPassword}}" />
      </view>
      <view class="feature-item">
        <button type="primary" bindtap="joinRoom">进入房间</button>
      </view>
    </view>
  </view>
</view>